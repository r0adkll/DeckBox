<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:paddingStart="@dimen/padding_normal"
    android:paddingEnd="@dimen/padding_normal"
    android:paddingBottom="@dimen/padding_small"
    android:paddingTop="16dp"
    >



    <SeekBar
        android:id="@+id/seekBar"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            style="@style/Widget.Deck.RangeModifier"
            android:id="@+id/modifier_greater_than"
            android:src="@drawable/ic_gt"
            />

        <ImageView
            style="@style/Widget.Deck.RangeModifier"
            android:id="@+id/modifier_greater_than_equal"
            android:src="@drawable/ic_gt_equal"
            />

        <ImageView
            style="@style/Widget.Deck.RangeModifier"
            android:id="@+id/modifier_less_than"
            android:src="@drawable/ic_lt"
            />

        <ImageView
            style="@style/Widget.Deck.RangeModifier"
            android:id="@+id/modifier_less_than_equal"
            android:src="@drawable/ic_lt_equal"
            />

    </LinearLayout>

</LinearLayout>